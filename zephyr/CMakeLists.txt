# Intentionally empty. This file is required by zephyr/module.yml's
# build.cmake declaration but the voyager_ps shield doesn't currently
# need any source files or configure-time hooks added to the Zephyr
# build. New configure-time patches or shield-side C sources can be
# wired in here when needed.
